import { createTheme } from '@mui/material/styles';

export const darkTheme = createTheme({
  palette: {
    background: {
      default: "#2c2c2c", // Dark background
    },
    text: {
      primary: "#f2ffff", // White text for readability
    },
  },
});
